#!/usr/bin/env php
<?php

use Symfony\Component\Console\Application;
use ThemePlate\Tester\TestCommand;

require_once getcwd() . '/vendor/autoload.php';

$application = new Application( 'ThemePlate Tester', '2.0.0' );

$application->add( new TestCommand() );

$application->run();
